namespace MWRMode.Patches.CharacterCreation
{
    // The broader TOR-style preview renderer patch set regressed shared previews in this project.
    // Keep the file so references stay stable, but leave it inactive until the race preview path
    // is reintroduced piece by piece.
    internal static class CharacterPreviewRaceFixPatches
    {
    }
}
